bind 0.0.0.0
port 6379
appendonly yes
save 60 1000
# Keep authentication and durable sync queues from being evicted under cache
# pressure. Size this cap against the production instance budget before rollout.
maxmemory 256mb
maxmemory-policy noeviction
maxmemory-samples 10
